# Get IP and Latency

This Go example resolves a domain to an IP address and measures average latency with ping.

## Environment Variables

This example does not require any example-specific environment variables.
