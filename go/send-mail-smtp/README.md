# Send Mail with SMTP

This Go example sends an email through an SMTP server. The LLM should provide the recipient address, subject, and body.

## Environment Variables

```sh
SMTP_HOST=<smtp-host>
SMTP_PORT=<smtp-port>
FROM_EMAIL=<sender-email>
```
