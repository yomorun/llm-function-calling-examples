# Timezone Calculator

This Go example converts a source time from one IANA time zone to another. The LLM should extract `timeString`, `sourceTimezone`, and `targetTimezone` before calling the function.

## Environment Variables

This example does not require any example-specific environment variables.
