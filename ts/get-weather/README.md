# Get Weather

This TypeScript example gets current weather for a city using OpenWeatherMap. The LLM should provide the city name and convert it to latitude and longitude before calling the function.

## Environment Variables

```sh
OPENWEATHERMAP_API_KEY=<your-openweathermap-api-key>
```
